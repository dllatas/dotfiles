---
- name: Symlink global gitconfig
  file:
    src: "{{ dotfiles_path }}/ansible/roles/git/files/.gitconfig"
    dest: "{{ ansible_facts['env']['HOME'] }}/.gitconfig"
    state: link
    force: true
